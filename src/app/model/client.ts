export class Client {
  constructor(public id : number, public nom: String, public prenom : String,
              public travail : String, public adresse : String, public email: String, public dateCreation : Date) {
  }
}
